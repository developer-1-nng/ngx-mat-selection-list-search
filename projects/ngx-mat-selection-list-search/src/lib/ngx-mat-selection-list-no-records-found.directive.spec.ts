import { NgxMatSelectionListNoRecordsFoundDirective } from './ngx-mat-selection-list-no-records-found.directive';

describe('NgxMatSelectionListNoRecordsFoundDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMatSelectionListNoRecordsFoundDirective();
    expect(directive).toBeTruthy();
  });
});
